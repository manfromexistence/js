# DX Bun Lane 1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Complete Lane 1 tasks 1-5 from `G:\Dx\js\PLAN.md` without touching other lanes or introducing fake runtime wiring.

**Architecture:** Keep real changes in the existing DX JS cache tooling. Build `dx-serialize` once, batch process inputs, emit a deterministic binary catalog sidecar, and expose validation-before-trust helper APIs for package/tsconfig/bunfig sidecars. Runtime Rust readers are gated by missing Bun workspace `rkyv`/`bytecheck`/`blake3` dependencies and must not be faked.

**Tech Stack:** PowerShell 7, Bun TypeScript tests, Bun repo Rust source scans.

---

### Task 1: Batch DX JS Machine Cache Generation

**Files:**

- Modify: `G:\Dx\js\scripts\dx-js-machine-cache.ps1`
- Modify: `G:\Dx\js\scripts\dx-js-machine-cache-script.test.ts`

- [ ] Add a one-time serializer executable resolver that runs Cargo metadata/build once.
- [ ] Replace per-input `cargo run` with bounded parallel `dx-serialize` process execution.
- [ ] Preserve all existing metadata/index/catalog validation.
- [ ] Prove the script does not call `cargo run` per input in the focused script test.

### Task 2: Trusted Sidecar Reader Contract

**Files:**

- Modify: `G:\Dx\js\scripts\dx-js-package-json-cache.ts`
- Modify: `G:\Dx\js\scripts\dx-js-package-json-cache.test.ts`

- [ ] Generalize cache lookup from package-only to trusted `package_json`, `tsconfig`, and `bunfig` kinds.
- [ ] Require source hash, machine hash, size, mtime, metadata schema, and bytecheck validation before returning trusted bytes.
- [ ] Keep the package-json fallback scanner unchanged.
- [ ] Add tests showing bytecheck is skipped when hash validation fails.

### Task 3: Binary Catalog Sidecar

**Files:**

- Modify: `G:\Dx\js\scripts\dx-js-machine-cache-index-contract.ts`
- Modify: `G:\Dx\js\scripts\dx-js-machine-cache-index-contract.test.ts`
- Modify: `G:\Dx\js\scripts\dx-js-machine-cache.ps1`
- Modify: `G:\Dx\js\scripts\dx-js-machine-cache-script.test.ts`

- [ ] Encode `catalog.machine` as a deterministic binary envelope beside `catalog.json`.
- [ ] Decode and validate the envelope in TypeScript tests.
- [ ] Keep JSON catalog as the compatibility/debug source of truth.

### Task 4: Rust Runtime Reader Feasibility

**Files:**

- Inspect only: `G:\Dx\js\src\resolver\package_json.rs`
- Inspect only: `G:\Dx\js\src\resolver\tsconfig_json.rs`
- Inspect only: `G:\Dx\js\src\bunfig\bunfig.rs`

- [ ] Verify whether Bun's Rust workspace already has `rkyv`, `bytecheck`, and `blake3`.
- [ ] Do not add broad Cargo dependencies inside this lane.
- [ ] Report blocked if a real reader cannot be wired without shared archived types and dependency changes.

### Task 5: Verification And Commit

**Files:**

- Stage only the lane-owned files above.

- [ ] Run focused Bun tests for the changed script contracts.
- [ ] Run targeted source scans for forbidden `.js/.cjs/.mjs` additions and runtime dependency assumptions.
- [ ] Commit only lane changes with an honest message.
