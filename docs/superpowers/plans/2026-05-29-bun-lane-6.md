# Bun Lane 6 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Complete PLAN.md tasks 26-30 with narrow, production-ready performance changes and focused verification.

**Architecture:** Keep each optimization local to its owning module and avoid broad build churn. Prefer existing Bun primitives, Rust RAII, generated/static data where it removes startup allocation, and opt-in behavior for cold/cache paths.

**Tech Stack:** Rust workspace crates, Bun TypeScript tests, PowerShell cache generator, `phf`, existing `bun_threading`, existing `bun_libdeflate_sys`, and external `G:\dx\serializer` CLI options already wired through `scripts/dx-js-machine-cache.ps1`.

---

### Task 26: Windows UTF-16 Which Path Reuse

**Files:**
- Modify: `src/which/lib.rs`

- [x] Inspect `which_win`, `search_bin_in_path`, and Windows-only UTF-16 conversion paths.
- [x] Avoid repeated UTF-16 length scans/conversions in the `$PATH` search by pre-encoding the bin path once per lookup while keeping the fallback length guard correct for non-ASCII paths.
- [x] Preserve absolute-path and relative-slash semantics, including extension probing.
- [x] Verify with `cargo check -p bun_which`.

### Task 27: Trusted Dependency Static Table Via phf

**Files:**
- Modify: `src/install/build.rs`
- Modify: `src/install/lockfile.rs`

- [x] Replace lazy runtime construction of the default trusted dependency map with build-script generated `phf` tables.
- [x] Keep `DEFAULT_TRUSTED_DEPENDENCIES_LIST` sorted for `bun pm default-trusted` output.
- [x] Preserve exact hash-collision safety for explicit `trustedDependencies`.
- [x] Verify static generation with `cargo check -p bun_install`.

### Task 28: libdeflate Decompressor Pooling

**Files:**
- Modify: `src/install/extract_tarball.rs`

- [x] Introduce a thread-local RAII wrapper for `libdeflate::Decompressor` so repeated install extraction does not allocate/destroy one per tarball.
- [x] Keep fallback to zlib on allocation failure or libdeflate decompression failure.
- [x] Keep output buffer clearing and max-size behavior unchanged.
- [x] Verify with `cargo check -p bun_install`.

### Task 29: Threadpool Stack-Bounds Audit

**Files:**
- Modify: `src/install/PackageManager.rs`

- [x] Audit pools that call `ThreadPool::init` and only run non-JS work.
- [x] Set `needs_stack_bounds = false` only where package manager tasks do not enter JSC/StackCheck paths.
- [x] Leave bundler/JS parse pools and global shared pools on stack bounds.
- [x] Verify with targeted Rust checks for touched crates.

### Task 30: Optional Compression Only For Cold Large Shards

**Files:**
- Modify: `scripts/dx-js-machine-cache.ps1`
- Modify: `scripts/dx-js-machine-cache-script.test.ts`

- [x] Add an explicit cold-large shard compression policy while keeping normal writes on `--no-compression`.
- [x] Only pass serializer `--lz4` or `--zstd` for large cold shard inputs above a threshold; keep hot package/tsconfig/bunfig defaults uncompressed.
- [x] Keep existing catalog schema and lookup keys unchanged because the serializer envelope is self-describing.
- [x] Verify with the focused DX machine cache script tests.

### Six Subagents

- [x] Subagent 1: Task 26 source audit and patch guidance.
- [x] Subagent 2: Task 27 source audit and patch guidance.
- [x] Subagent 3: Task 28 source audit and patch guidance.
- [x] Subagent 4: Task 29 source audit and patch guidance.
- [x] Subagent 5: Task 30 source audit and patch guidance.
- [x] Subagent 6: Integration/spec/code-quality review of lane 6 diff was spawned as the final reviewer; the tool later returned `not_found` when waiting, so no final reviewer report was available.

### Final Verification

- [x] Run the lightest targeted checks that cover touched code.
- [x] Run `git diff --check`.
- [x] Re-read PLAN.md lane tasks and confirm each is complete.
- [x] Commit only lane-scoped tracked changes.
