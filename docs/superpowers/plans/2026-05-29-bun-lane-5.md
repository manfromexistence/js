# Bun Lane 5 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Complete PLAN.md tasks 21-25 with narrow, production-ready Rust performance changes and focused verification.

**Architecture:** Keep each optimization local to its owning hot path. Preserve Bun's Rust port semantics, legacy Zig compatibility where comments identify it as source truth, and existing cache/resolver/printer behavior while reducing avoidable scans, zeroing, and heap churn.

**Tech Stack:** Rust workspace crates, Bun resolver/runtime/printer/source-map internals, existing `bun_sys` vectored IO, existing `itoa`, optional `ryu`, and targeted Cargo checks only.

---

### Task 21: Exact-Key Index For Package Exports And Imports

**Files:**
- Modify: `src/resolver/package_json.rs`
- Verify: resolver-focused check or `cargo check -p bun_resolver`

- [x] Inspect `EntryDataMap`, `Visitor::visit`, `Entry::value_for_key`, and `ESModule::resolve_imports_exports`.
- [x] Add an exact-key index for non-pattern exports/imports map entries without changing insertion-order behavior or duplicate-key winner behavior.
- [x] Keep expansion key sorting and condition-map iteration unchanged.
- [x] Verify the resolver crate or the narrowest available resolver test/check.

### Task 22: Vectored Runtime Transpiler Cache Reads With MaybeUninit

**Files:**
- Modify: `src/jsc/RuntimeTranspilerCache.rs`
- Verify: `cargo check` for the owning crate or a narrow runtime transpiler cache check

- [x] Inspect cache file layout, `Metadata`, `Entry::load`, `pread_box`, and `sys::preadv` platform wrappers.
- [x] Replace repeated boxed payload `pread_all` calls where practical with vectored reads into uninitialized buffers, preserving hash validation and short-read errors.
- [x] Do not change the cache wire format or `EXPECTED_VERSION` unless the on-disk format changes.
- [x] Verify with the narrowest check that type-checks the touched runtime cache path.

### Task 23: Targeted Sourcemap Cache Invalidation

**Files:**
- Modify: `src/jsc/SavedSourceMap.rs`
- Inspect: `src/sourcemap/InternalSourceMap.rs`
- Verify: source-map or JSC crate check

- [x] Inspect `SavedSourceMap::put_value`, provider removal paths, `FindCache`, and `ParsedSourceMap::internal` ownership.
- [x] Replace all-cache invalidation on map updates with invalidation of only the replaced path/blob where possible.
- [x] Preserve safety before freeing internal sourcemap blobs and keep provider removal cache-safe.
- [x] Verify with the narrowest check that covers the touched source-map/JSC surface.

### Task 24: ryu + itoa Printer Number Formatting Audit

**Files:**
- Modify: `Cargo.toml` if `ryu` is added as a workspace dependency
- Modify: `src/js_printer/Cargo.toml` if needed
- Modify: `src/js_printer/lib.rs`
- Inspect: `src/ast/e.rs`
- Verify: `cargo check -p bun_js_printer`

- [x] Inspect JS printer float/int formatting and existing `bun_core::fmt::itoa` usage.
- [x] Keep integer output on the existing `itoa` path.
- [x] Replace the generic Rust `format_args!("{}", float)` fallback with a shortest-round-trip float formatter that preserves JS-printer semantics.
- [x] Verify the printer crate and avoid broader formatting churn.

### Task 25: Path Buffer Pool Fixed Storage

**Files:**
- Modify: `src/paths/path_buffer_pool.rs`
- Verify: `cargo check -p bun_paths`

- [x] Inspect the thread-local path buffer pool and previous dynamic pool storage.
- [x] Replace dynamic pool storage with fixed-capacity storage while preserving the four-buffer cap, RAII return behavior, and Windows `PathBuffer` / `WPathBuffer` behavior.
- [x] Keep cache-miss allocation semantics and no hot-path zeroing beyond current behavior.
- [x] Verify with `cargo check -p bun_paths`.

### Six Subagents

- [x] Subagent 1: Task 21 resolver exact-key index source audit and patch guidance.
- [x] Subagent 2: Task 22 runtime transpiler cache vectored-read source audit and patch guidance.
- [x] Subagent 3: Task 23 sourcemap targeted invalidation source audit and patch guidance.
- [x] Subagent 4: Task 24 JS printer number formatting audit and patch guidance.
- [x] Subagent 5: Task 25 path buffer pool fixed-storage source audit and patch guidance.
- [x] Subagent 6: Integration/spec/code-quality review of lane 5 diff.

### Final Verification

- [x] Run targeted checks for touched crates only.
- [x] Run `git diff --check`.
- [x] Re-read PLAN.md lane tasks and confirm each is complete or honestly marked incomplete.
- [x] Commit only lane-scoped tracked changes.
