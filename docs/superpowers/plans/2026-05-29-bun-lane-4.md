# Bun Lane 4 Implementation Plan

> Required workflow: this lane uses Superpowers planning, exactly six GPT-5.5 xhigh subagents, focused verification, and a lane-scoped commit.

**Goal:** Complete PLAN.md tasks 16-20 with production-ready improvements to DX machine-cache metadata and package.json exports/imports storage, without broad builds or unrelated cleanup.

**Lane:** 4

**Task range:** 16-20

**Assigned tasks:**
- Task 16: Add packed shard-header metadata for the future `.dx/js` shard store.
- Task 17: Remove default no-compression serializer forcing and keep the serializer envelope path explicit.
- Task 18: Intern repeated JSON keys in package machine-cache sidecar metadata.
- Task 19: Reduce exports/imports string allocation pressure in `package_json.rs`.
- Task 20: Move exports/imports map storage toward `MultiArrayList` column layout.

## Scope

- Modify only lane-owned files and adjacent tests.
- Do not touch external `G:\dx\serializer` files because they are outside this repo's commit boundary.
- Keep cache lookup keys stable.
- Keep exports/imports resolver behavior stable.
- Avoid `.js`, `.cjs`, or `.mjs` helper scripts.

## Implementation Steps

- [ ] Run focused baseline script tests and source scans.
- [ ] Delegate exactly six subagents:
  - [ ] Subagent 1: task 16 catalog shard-header contract.
  - [ ] Subagent 2: task 17 PowerShell serializer invocation and tests.
  - [ ] Subagent 3: task 18 package sidecar key-intern contract and tests.
  - [ ] Subagent 4: tasks 19-20 Rust exports/imports storage patch.
  - [ ] Subagent 5: Rust storage design review for package_json changes.
  - [ ] Subagent 6: final lane diff review after integration.
- [ ] Implement and integrate TS/PowerShell machine-cache changes.
- [ ] Implement and integrate Rust package_json storage changes with RAII-safe ownership.
- [ ] Run focused tests/checks for touched areas.
- [ ] Run `git diff --check`.
- [ ] Commit only lane-scoped changes.

## Verification Targets

- `bun test --timeout 20000 ./scripts/dx-js-package-json-cache.test.ts ./scripts/dx-js-machine-cache-index-contract.test.ts ./scripts/dx-js-machine-cache-script.test.ts`
- Targeted Rust check for `bun_resolver` if the package_json storage change compiles within the lightweight budget.
- `git diff --check`

## Risk Notes

- `MultiArrayList` drops slab storage only, so any owning-column use must call `drop_elements()` exactly once from an owning wrapper.
- `package_json.rs` source-backed string changes must not use unsafe lifetime extension unless the source ownership contract is explicit and mechanically preserved.
- The serializer crate is outside the Bun repo, so lane work can wire Bun-side catalog/script contracts but should not claim external serializer internals were changed unless separately modified.
